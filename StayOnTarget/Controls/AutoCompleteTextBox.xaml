<UserControl x:Class="StayOnTarget.Controls.AutoCompleteTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Grid>
        <TextBox x:Name="InputTextBox"
                 Text="{Binding Text, ElementName=Root, UpdateSourceTrigger=PropertyChanged, Delay=150}"
                 Style="{DynamicResource {x:Type TextBox}}" />

        <Popup x:Name="SuggestionPopup"
               PlacementTarget="{Binding ElementName=InputTextBox}"
               Placement="Bottom"
               IsOpen="False"
               StaysOpen="False"
               AllowsTransparency="True"
               Focusable="False">
            <Border Background="{DynamicResource SurfaceBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderColorBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    MinWidth="{Binding ElementName=InputTextBox, Path=ActualWidth}"
                    MaxHeight="200"
                    Margin="0,2,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3" />
                </Border.Effect>

                <ListBox x:Name="SuggestionListBox"
                         ItemsSource="{Binding ElementName=Root, Path=_filteredView}"
                         DisplayMemberPath="{Binding ElementName=Root, Path=DisplayMemberPath}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="6,4" />
                            <Setter Property="Cursor" Value="Hand" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>